// Die globalen Funktionen für das package 'memo-notify' liegen jetzt im
//     package Ordner 'Groupware\memo-notify\'
//
// Sofern dieses Programm noch "beim Start von be32" geladen wird, bitte umstellen auf
// den neuen package-loader. Hier erfolgt eine automatische Umleitung, damit nach 
// package-update weiterhin die globals geladen werden.

Call('Groupware\memo-notify\.globals\_PackageLoader'),
